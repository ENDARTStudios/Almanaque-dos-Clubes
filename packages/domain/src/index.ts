/**
 * Almanaque dos Clubes — Domínio compartilhado.
 *
 * Aqui vivem tipos, validadores Zod e constantes que podem ser
 * reutilizados tanto pela API (apps/api) quanto pelo frontend (apps/web).
 */

export * from './club.js';
export * from './errors.js';
