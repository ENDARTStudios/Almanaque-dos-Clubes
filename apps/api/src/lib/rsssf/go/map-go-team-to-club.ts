/** T448b-2d GO — time→clube (reusa o núcleo genérico: match exato por QID/alias, SEM fuzzy). */
export {
  mapTeamToClub as mapGoTeamToClub,
  buildClubIndex as buildGoClubIndex,
  normalizeTeamName as normalizeGoTeamName,
} from '../map-team-to-club.js';
