/** T448b-2d GO — decode de tabela (reusa o núcleo genérico RSSSF). */
export {
  decodeLegacyTable as decodeGoTable,
  fixtureToText as goFixtureToText,
  decodeLegacyBytes,
  htmlToText,
} from '../decode-legacy-table.js';
