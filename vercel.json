{
  "framework": "nextjs",
  "installCommand": "corepack enable && corepack prepare pnpm@11 --activate && pnpm install",
  "buildCommand": "pnpm --filter web build",
  "outputDirectory": "apps/web/.next"
}
